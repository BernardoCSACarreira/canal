# Default local stack for Go (ingestion-edge) + Python (control plane).
# Host ports align with apps/ingestion-ui Vite defaults: data plane :8080, control :8091.
# ingestion-edge-go: http://127.0.0.1:8080/health
# ingestion-control-plane: http://127.0.0.1:8091/health
services:
  ingestion-edge-go:
    build:
      context: ./services/ingestion-edge-go
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"

  ingestion-control-plane:
    build:
      context: ./services/ingestion-control-plane
    ports:
      - "8091:8091"
